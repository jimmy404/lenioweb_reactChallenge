import { css } from 'styled-components';

const styles = {
  ComicPageContainer: css`
    height: calc(100vh - 80px);
    margin-top: 80px;
    padding: 60px;
    width: 100vw;
  `
};

export default styles;
